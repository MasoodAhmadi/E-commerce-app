import React from 'react';

function Header() {
  return (
    <header>
      <h1>eCommerce Store</h1>
      {/* Add navigation links, user authentication, etc. */}
    </header>
  );
}

export default Header;
