{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test:coveralls": "jest --verbose ./src/test --coverage && coveralls < coverage/lcov.info",
    "test": "jest --verbose ./src/test --coverage && cat ./coverage/lcov.info | coveralls-next",
    "test1": "jest --verbose ./src/test --coverage && coveralls < coverage/lcov.info",
    "test2": "coveralls < coverage/lcov.info",
    "test_demo": "jest --verbose ./src/test --coverage"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/preset-env": "^7.23.3",
    "chai": "^4.3.10",
    "coveralls": "^3.1.1",
    "coveralls-next": "^4.2.0",
    "jest": "^29.7.0",
    "mocha": "^10.2.0",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
